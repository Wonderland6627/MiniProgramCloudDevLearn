# NBStudy 项目文档

## 项目概述

这是一个基于微信小程序云开发的自习室管理系统（宁博自习室），主要用于学生信息管理、套餐管理、座位管理等业务功能。

## 技术架构

### 前端技术栈
- **框架**：微信小程序原生框架
- **云开发**：微信云开发（云数据库、云函数、云存储）
- **SDK**：@cloudbase/wx-cloud-client-sdk（数据模型操作）
- **云环境ID**：cloud1-8gewsvyn8efe62b8

### 后端技术栈
- **云函数**：Node.js + wx-server-sdk
- **数据库**：微信云数据库（MongoDB）

## 项目结构

```
├── miniprogram/          # 小程序前端代码
│   ├── pages/           # 页面目录
│   │   └── nbstudy/    # 业务页面（登录、学生主页、管理员页面等）
│   ├── components/      # 组件目录
│   ├── utils/           # 工具函数
│   ├── app.js          # 小程序入口
│   └── dataMgr.js      # 数据管理器
├── cloudfunctions/      # 云函数目录
│   └── quickstartFunctions/  # 统一入口云函数
└── project.config.json  # 项目配置
```

## 核心业务流程

### 1. 登录流程
- **学生登录**：微信授权登录 → 获取OPENID → 检查学生信息是否存在 → 不存在则创建/绑定
- **管理员登录**：登录页长按图标3次 → 进入管理员模式

### 2. 学生注册流程
- 新用户：创建学生记录（OPENID绑定）
- 老用户：通过手机号绑定已有记录（OPENID从'UNKNOWN'更新为实际OPENID）

### 3. 学生信息管理
- 基础信息：姓名、手机号、性别、生日、学校、学习目标
- 套餐信息：座位类型、时长类型、起始日期、到期日期
- 头像上传：云存储保存，返回临时链接

### 4. 管理员功能
- 学生列表管理（所有学生/套餐内学生/待绑定学生）
- 学生信息编辑（包括套餐信息）
- 套餐价格管理
- 门店门禁密码管理

## 数据表结构

### students（学生表）
```javascript
{
  _id: String,                    // 文档ID
  OPENID: String,                 // 微信OPENID（'UNKNOWN'表示待绑定）
  studentName: String,            // 学生姓名
  phone: String,                  // 手机号
  gender: Number,                 // 性别（0:女生, 1:男生）
  birthday: Number,               // 生日（时间戳）
  school: String,                 // 学校
  studyGoal: String,              // 学习目标
  avatarUrl: String,              // 头像URL
  seatName: String,               // 座位名称
  seatType: String,               // 座位类型（'0'-'4'对应Empty/VIP/A/B/C）
  durationType: String,          // 时长类型（'0'-'7'对应次卡/周卡/半月卡/月卡/双月卡/季卡/半年卡/年卡）
  packageStartDate: Number,       // 套餐起始日期（时间戳）
  packageExpirationDate: Number,  // 套餐到期日期（时间戳）
  storeID: Number,                // 门店ID
  comment: String,                // 备注
  joinedDate: Number              // 加入日期（时间戳）
}
```

### stores（门店表）
```javascript
{
  storeID: Number,                // 门店ID
  accessControlPassword: String   // 门禁密码
}
```

### packages（套餐表）
```javascript
{
  seatType: String,               // 座位类型
  durationType: String,           // 时长类型
  price: Number,                  // 价格
  giftDayCount: Number           // 赠送天数
}
```

### feedbacks（反馈表）
```javascript
{
  feedbackOPENID: String,         // 反馈者OPENID
  feedbackTime: Number,           // 反馈时间（时间戳）
  feedbackContent: String         // 反馈内容
}
```

### remoteconfig（远程配置表）
```javascript
{
  configJson: Object              // 配置JSON（包含programMode、swiperImagesList等）
}
```

## 核心云函数

### quickstartFunctions（统一入口）
- `getOpenId`：获取微信OPENID
- `getJSCode2Session`：通过code换取session
- `checkLogin`：检查登录状态
- `createStudent`：创建学生记录
- `bindStudent`：绑定学生（通过手机号）
- `updateStudent`：更新学生信息（通过OPENID）
- `updateStudentByDocID`：更新学生信息（通过_id）
- `updatePwd`：更新门店门禁密码
- `updatePackages`：批量更新套餐价格

## 隐藏规则与业务逻辑

### 1. 待绑定机制
- `OPENID = 'UNKNOWN'` 的记录表示待绑定学生（管理员预先创建，等待学生通过手机号绑定）

### 2. 有效套餐判断
```javascript
isValidPackage = packageExpirationDate > 当前时间 && seatName !== ''
```
- 套餐未到期 + 有座位名称 = 有效套餐用户
- 有效套餐用户可查看WiFi密码、门禁密码等

### 3. 临时用户判断
```javascript
isTemp = !packageExpirationDate
```
- 没有套餐到期日期 = 临时用户（次卡用户）

### 4. 管理员登录
- 登录页长按图标3次触发管理员登录
- 管理员模式：`getApp().isAdmin = true`

### 5. 数据权限控制
- WiFi密码、门禁密码：仅有效套餐用户可查看
- 学生信息编辑：学生只能编辑自己的基础信息
- 管理员可编辑所有学生信息（包括套餐信息）

### 6. 手机号唯一性
- 保存学生信息时检查手机号是否已被其他学生使用
- 同一手机号不能重复注册

### 7. 昵称生成规则
- 根据学生_id的hash值，从预设的20个英文昵称中选择一个

### 8. 审核模式限制（programMode = 'private'）
**背景**：微信小程序审核要求个人开发者不能收集用户个人信息，因此通过`remoteconfig`表的`programMode`字段控制审核模式。

**审核模式下的限制**：
- **学生编辑基础信息页面**：
  - 隐藏所有个人信息收集字段（姓名、手机号、性别、生日、学校、学习目标）
  - 仅显示昵称输入框
  - 保存时调用`saveInfoPrivately()`方法，手机号为空时自动生成GUID避免收集真实手机号
  
- **管理员编辑学生页面**：
  - 隐藏所有个人信息编辑功能
  - 仅显示昵称（只读），不可编辑
  
- **管理员学生列表页面**：
  - 学生列表中显示昵称（`item.nickname`）而不是真实姓名（`item.studentName`）
  
- **学生个人中心页面**：
  - 隐藏"修改个人基础信息"菜单项，避免进入信息收集页面
  
- **学生绑定状态查询页面**：
  - 将"手机号查询"改为"ID查询"
  - 查询功能被禁用，点击查询直接返回登录页
  
- **学生主页**：
  - 不刷新轮播图列表和工具列表（避免动态加载内容）

**配置方式**：
- 通过`remoteconfig`表的`configJson.programMode`字段控制
- 默认值为`'private'`（审核模式）
- 审核通过后可改为其他值（如`'public'`）恢复完整功能
- 配置每60秒自动拉取一次

## 关键工具模块

### dataMgr.js（数据管理器）
- `fetchStudentInfo()`：拉取学生信息
- `setStudentInfo()`：设置学生信息
- `parseStudentInfo()`：解析学生信息（添加格式化字段、有效性判断等）
- `clear()`：清空数据

### commonFunctions.js（通用功能）
- `showWiFiModal()`：显示WiFi密码
- `fetchAccessControlPwd()`：获取门禁密码
- `createFeedback()`：创建反馈
- `getMapperNickname()`：获取映射昵称

### remoteConfig.js（远程配置）
- 启动时拉取一次，之后每60秒拉取一次
- 配置项：
  - `programMode`：程序模式（`'private'`=审核模式，限制用户信息收集；其他值=正常模式）
  - `swiperImagesList`：轮播图列表

## 页面路由

### 学生端
- `/pages/nbstudy/login/index`：登录页
- `/pages/nbstudy/student-main/index`：学生主页
- `/pages/nbstudy/student-profile/index`：个人中心
- `/pages/nbstudy/student-editBasicInfo/index`：编辑基础信息
- `/pages/nbstudy/student-checkBindStatus/index`：检查绑定状态
- `/pages/nbstudy/student-contact-us/index`：联系我们
- `/pages/nbstudy/student-feedback/index`：意见反馈

### 管理员端
- `/pages/nbstudy/admin-main/index`：管理员主页（学生列表）
- `/pages/nbstudy/admin-edit/index`：管理员编辑页
- `/pages/nbstudy/admin-editStudent/index`：编辑学生信息
- `/pages/nbstudy/admin-editStore/index`：编辑门店信息
- `/pages/nbstudy/admin-editACPassword/index`：编辑门禁密码
- `/pages/nbstudy/admin-editPackage/index`：编辑套餐价格
- `/pages/nbstudy/admin-editSeat/index`：编辑座位信息

## 注意事项

1. **环境配置**：云环境ID在`app.js`和`envList.js`中配置
2. **隐私协议**：登录页需要用户同意隐私协议才能登录
3. **版本更新**：小程序启动时检查版本更新
4. **数据同步**：学生信息变更后需要调用`dataMgr.setStudentInfo()`同步本地数据
5. **错误处理**：关键操作都有错误处理和用户提示
6. **审核模式**：提交审核前需确保`remoteconfig`表中`programMode`为`'private'`，审核通过后可改为正常模式

