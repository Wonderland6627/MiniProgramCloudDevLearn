# 签到功能设计方案

## 一、功能概述

为admin模式添加日签到管理功能，用于记录和管理学生的每日签到情况，并支持次卡统计。

### 核心需求
1. **日历签到管理页**：可以查看和管理（添加/删除）选中当日的学生列表
2. **学生详情页签到历史**：每个学生的管理详情页能看到所有签到过的日期
3. **次卡统计**：次卡每次购买10次，签到1天算1次，需要显示每个学生剩余次卡次数
4. **学生端**：暂不需要主动签到功能，全部由admin控制

---

## 二、数据表结构设计

### 2.1 新增表：attendance_records（签到记录表）

用于存储学生的每日签到记录。

```javascript
{
  _id: String,                    // 文档ID（自动生成）
  studentId: String,              // 学生_id（关联students表）
  attendanceDate: String,         // 签到日期（格式：YYYY-MM-DD，用于查询和去重）
  attendanceTimestamp: Number,   // 签到时间戳（精确到毫秒，用于排序）
  createdAt: Number,              // 记录创建时间戳
  createdBy: String,              // 创建者标识（可选，记录是哪个admin操作的）
  comment: String                 // 备注（可选，如"补签"等）
}
```

**索引设计**：
- 复合索引：`{ studentId: 1, attendanceDate: 1 }` - 确保同一学生同一天只能有一条签到记录
- 单字段索引：`attendanceDate` - 用于按日期查询签到学生列表

**业务规则**：
- 同一学生同一天只能有一条签到记录（通过复合索引保证唯一性）
- 删除签到记录时，需要同步更新学生的剩余次卡次数（+1）

---

### 2.2 修改表：students（学生表）

在现有students表中添加次卡相关字段。

**新增字段**：
```javascript
{
  // ... 现有字段 ...
  
  // 次卡相关字段
  cardRemainingCount: Number      // 次卡剩余次数（默认0）
}
```

**字段说明**：
- `cardRemainingCount`：当前剩余次数，签到时会-1，删除签到时会+1
- 注意：次卡购买功能暂不考虑，剩余次数需要管理员手动设置或通过其他方式管理

**初始化规则**：
- 新增学生时，次卡相关字段默认为0
- 签到成功时，`cardRemainingCount`减1（如果>0）
- 删除签到记录时，`cardRemainingCount`加1

---

## 三、客户端页面设计

### 3.1 新增页面：admin-attendance-calendar（日历签到管理页）

**页面路径**：`/pages/nbstudy/admin-attendance-calendar/index`

**功能描述**：
- 显示日历组件，可以选择日期
- 选中日期后，显示该日期的签到学生列表
- 可以添加学生到该日期的签到列表
- 可以删除该日期的签到记录
- 显示当日签到学生总数

**页面布局**：
```
┌─────────────────────────────┐
│  日历签到管理                │
├─────────────────────────────┤
│  [日历组件]                  │
│  2024-01-15 (周一)          │
├─────────────────────────────┤
│  今日签到学生 (5人)         │
├─────────────────────────────┤
│  [学生列表]                  │
│  ┌─────────────────────┐    │
│  │ 头像 | 姓名 | [删除]│    │
│  └─────────────────────┘    │
│  ┌─────────────────────┐    │
│  │ 头像 | 姓名 | [删除]│    │
│  └─────────────────────┘    │
│  ...                         │
├─────────────────────────────┤
│  [+ 添加签到学生]            │
└─────────────────────────────┘
```

**交互流程**：
1. 页面加载时，默认选中今天
2. 点击日历日期，切换选中日期
3. 显示选中日期的签到学生列表
4. 点击"添加签到学生"，弹出学生选择器（可搜索）
5. 选择学生后，添加到签到列表（调用云函数）
6. 点击学生项的"删除"按钮，删除该签到记录（调用云函数）
7. 删除时，如果该学生是次卡用户，自动更新剩余次数

**数据展示**：
- 学生列表显示：头像、姓名（或昵称，根据programMode）、剩余次卡次数（如果是次卡用户）
- 次卡用户显示剩余次数，如："剩余3次"

---

### 3.2 修改页面：admin-editStudent（学生详情编辑页）

**修改内容**：
在现有学生详情页添加"签到记录"模块。

**位置**：在套餐信息下方，备注信息上方

**布局设计**：
```
┌─────────────────────────────┐
│  学生详情                    │
├─────────────────────────────┤
│  [基础信息区域]              │
│  [套餐信息区域]              │
├─────────────────────────────┤
│  签到记录                    │
│  ┌─────────────────────┐    │
│  │ 次卡剩余: 3次        │    │
│  │ 累计签到: 15天      │    │
│  └─────────────────────┘    │
│  ┌─────────────────────┐    │
│  │ 2024-01-15 (周一)   │    │
│  │ 2024-01-14 (周日)   │    │
│  │ 2024-01-13 (周六)   │    │
│  │ ...                 │    │
│  └─────────────────────┘    │
├─────────────────────────────┤
│  [备注信息区域]              │
└─────────────────────────────┘
```

**功能说明**：
- 显示次卡剩余次数（仅次卡用户显示，durationType='0'）
- 显示累计签到天数
- 显示最近N条签到记录（如最近10条），按日期倒序
- 支持展开显示更多签到记录（或使用滚动加载）

**数据获取**：
- 页面加载时，查询该学生的所有签到记录
- 按`attendanceTimestamp`倒序排列
- 计算累计签到天数（去重后的日期数量）

---

### 3.3 修改页面：admin-main（管理员主页）

**修改内容**：
在学生列表项中显示次卡剩余次数（如果是次卡用户）。

**显示位置**：
在学生信息卡片中，套餐信息下方添加：
```
次卡剩余: 3次  // 仅当durationType='0'时显示
```

---

## 四、云函数设计

### 4.1 新增云函数：attendance相关操作

在`cloudfunctions/quickstartFunctions/index.js`中添加新的case分支。

#### 4.1.1 addAttendance（添加签到记录）

**云函数路径**：`cloudfunctions/quickstartFunctions/attendance/addAttendance/index.js`

**功能**：
- 为学生添加指定日期的签到记录
- 检查是否已存在该日期的签到记录（防止重复）
- 如果是次卡用户，更新剩余次数（-1）
- 返回操作结果

**参数**：
```javascript
{
  type: 'addAttendance',
  data: {
    studentId: String,           // 学生_id
    attendanceDate: String,      // 签到日期 YYYY-MM-DD
    comment: String              // 可选，备注
  }
}
```

**返回**：
```javascript
{
  code: 0,                       // 0=成功，-1=失败，-2=已存在
  errMsg: String,
  data: {
    attendanceId: String         // 创建的签到记录_id
  }
}
```

**业务逻辑**：
1. 检查该学生该日期是否已有签到记录
2. 如果已存在，返回`code: -2`
3. 创建签到记录
4. 查询学生信息，判断是否为次卡用户（`durationType === '0'`）
5. 如果是次卡用户且`cardRemainingCount > 0`，则`cardRemainingCount - 1`
6. 更新学生信息
7. 返回成功

---

#### 4.1.2 deleteAttendance（删除签到记录）

**云函数路径**：`cloudfunctions/quickstartFunctions/attendance/deleteAttendance/index.js`

**功能**：
- 删除指定学生的指定日期签到记录
- 如果是次卡用户，更新剩余次数（+1）
- 返回操作结果

**参数**：
```javascript
{
  type: 'deleteAttendance',
  data: {
    attendanceId: String,        // 签到记录_id（或studentId + attendanceDate）
    studentId: String,           // 学生_id（用于更新剩余次数）
    attendanceDate: String       // 签到日期 YYYY-MM-DD
  }
}
```

**返回**：
```javascript
{
  code: 0,                       // 0=成功，-1=失败
  errMsg: String
}
```

**业务逻辑**：
1. 删除签到记录
2. 查询学生信息，判断是否为次卡用户
3. 如果是次卡用户，则`cardRemainingCount + 1`
4. 更新学生信息
5. 返回成功

---

#### 4.1.3 getAttendanceByDate（按日期查询签到记录）

**云函数路径**：`cloudfunctions/quickstartFunctions/attendance/getAttendanceByDate/index.js`

**功能**：
- 查询指定日期的所有签到学生列表
- 返回学生基本信息（用于日历页展示）

**参数**：
```javascript
{
  type: 'getAttendanceByDate',
  data: {
    attendanceDate: String       // 签到日期 YYYY-MM-DD
  }
}
```

**返回**：
```javascript
{
  code: 0,
  data: {
    records: [                   // 签到记录列表
      {
        _id: String,             // 签到记录_id
        studentId: String,       // 学生_id
        studentName: String,     // 学生姓名
        avatarUrl: String,       // 头像URL
        cardRemainingCount: Number, // 剩余次卡次数（仅次卡用户）
        attendanceTimestamp: Number,
        comment: String
      },
      ...
    ],
    total: Number                // 总数量
  }
}
```

**业务逻辑**：
1. 根据`attendanceDate`查询签到记录
2. 关联查询学生信息（通过`studentId`）
3. 组装返回数据，包含学生基本信息和剩余次卡次数
4. 返回列表

---

#### 4.1.4 getAttendanceByStudent（按学生查询签到记录）

**云函数路径**：`cloudfunctions/quickstartFunctions/attendance/getAttendanceByStudent/index.js`

**功能**：
- 查询指定学生的所有签到记录
- 支持分页和排序
- 返回签到日期列表和统计信息

**参数**：
```javascript
{
  type: 'getAttendanceByStudent',
  data: {
    studentId: String,           // 学生_id
    pageSize: Number,            // 可选，每页数量，默认20
    pageNum: Number,             // 可选，页码，默认1
    startDate: String,           // 可选，开始日期 YYYY-MM-DD
    endDate: String             // 可选，结束日期 YYYY-MM-DD
  }
}
```

**返回**：
```javascript
{
  code: 0,
  data: {
    records: [                   // 签到记录列表
      {
        _id: String,
        attendanceDate: String,
        attendanceTimestamp: Number,
        comment: String
      },
      ...
    ],
    total: Number,               // 总记录数
    totalDays: Number,           // 总签到天数（去重后的日期数量）
    cardRemainingCount: Number   // 剩余次卡次数（仅次卡用户）
  }
}
```

**业务逻辑**：
1. 根据`studentId`查询签到记录
2. 如果提供了日期范围，添加日期过滤条件
3. 按`attendanceTimestamp`倒序排列
4. 计算总签到天数（去重后的日期数量）
5. 查询学生信息，获取剩余次卡次数
6. 返回分页数据和统计信息

---

---

## 五、数据管理流程

### 5.1 签到流程

```
1. Admin在日历页选择日期
2. 点击"添加签到学生"
3. 选择学生（可搜索）
4. 调用 addAttendance 云函数
5. 云函数检查是否已存在该日期的签到记录
6. 如果不存在，创建签到记录
7. 如果是次卡用户，cardRemainingCount - 1
8. 更新学生信息
9. 返回成功，刷新日历页列表
```

### 5.2 删除签到流程

```
1. Admin在日历页或学生详情页点击删除
2. 确认删除操作
3. 调用 deleteAttendance 云函数
4. 云函数删除签到记录
5. 如果是次卡用户，cardRemainingCount + 1
6. 更新学生信息
7. 返回成功，刷新页面列表
```

### 5.3 查询签到记录流程

```
1. 日历页：按日期查询 → getAttendanceByDate
2. 学生详情页：按学生查询 → getAttendanceByStudent
3. 返回签到记录列表和统计信息
```

---

## 六、页面路由配置

### 6.1 新增页面路由

在`miniprogram/app.json`的`pages`数组中添加：

```json
{
  "pages": [
    // ... 现有页面 ...
    "pages/nbstudy/admin-attendance-calendar/index"
  ]
}
```

### 6.2 页面跳转

**从admin-main跳转到日历页**：
```javascript
wx.navigateTo({
  url: '/pages/nbstudy/admin-attendance-calendar/index'
})
```

---

## 七、UI/UX设计建议

### 7.1 日历组件

- 使用微信小程序原生`picker`组件选择日期，或使用第三方日历组件
- 选中日期高亮显示
- 有签到记录的日期显示标记（如小圆点）

### 7.2 学生列表

- 使用与`admin-main`页面一致的学生卡片样式
- 次卡用户显示剩余次数，用不同颜色标识（如绿色=充足，橙色=较少，红色=即将用完）
- 删除按钮使用图标按钮，点击后弹出确认对话框

### 7.3 统计信息

- 使用卡片式布局展示统计信息
- 数字使用大号字体突出显示
- 使用图标辅助说明

### 7.4 交互反馈

- 所有操作都有loading提示
- 操作成功/失败都有toast提示
- 删除操作需要二次确认
- 添加签到前检查是否已存在，如果存在给出提示

---

## 八、数据安全与权限

### 8.1 权限控制

- 所有签到相关功能仅在admin模式下可用
- 云函数中验证调用者身份（通过OPENID或admin标识）
- 学生端不显示签到相关功能

### 8.2 数据校验

- 添加签到前检查日期格式
- 检查学生是否存在
- 检查是否重复签到
- 删除签到前检查记录是否存在

### 8.3 数据一致性

- 使用数据库事务（如果支持）确保签到记录和次卡次数的同步更新
- 如果事务不支持，使用先更新再回滚的机制
- 定期检查数据一致性（可选，通过定时任务）

---

## 九、性能优化建议

### 9.1 查询优化

- 使用索引加速查询（`studentId + attendanceDate`复合索引）
- 分页加载签到记录，避免一次性加载大量数据
- 缓存学生基本信息，减少重复查询

### 9.2 页面优化

- 日历页按需加载签到记录（切换日期时才加载）
- 学生详情页的签到记录使用懒加载或分页
- 使用虚拟列表（如果记录数量很大）

### 9.3 数据同步

- 操作成功后立即刷新相关页面数据
- 使用事件总线（eventBus）通知其他页面更新数据

---

## 十、扩展功能（可选）

### 10.1 批量操作

- 支持批量添加签到（选择多个学生）
- 支持批量删除签到
- 支持按时间段批量导出签到记录

### 10.2 统计报表

- 按月份统计签到情况
- 按学生统计签到频率
- 次卡使用情况统计

### 10.3 提醒功能

- 次卡剩余次数不足时提醒admin
- 学生长时间未签到时提醒admin

### 10.4 补签功能

- 支持为历史日期补签
- 补签记录添加特殊标记

---

## 十一、实施步骤建议

### 第一阶段：基础功能
1. 创建`attendance_records`表
2. 修改`students`表，添加次卡字段
3. 实现`addAttendance`和`deleteAttendance`云函数
4. 创建日历签到管理页（基础功能）

### 第二阶段：完善功能
1. 实现`getAttendanceByDate`和`getAttendanceByStudent`云函数
2. 修改学生详情页，添加签到记录展示
3. 完善UI和交互

### 第三阶段：优化和扩展
1. 添加数据校验和错误处理
2. 性能优化
3. 添加统计功能
4. 测试和bug修复

---

## 十二、注意事项

1. **数据迁移**：如果已有学生数据，需要为现有学生初始化次卡字段（默认为0）
2. **日期格式**：统一使用`YYYY-MM-DD`格式存储日期，便于查询和比较
3. **时区问题**：签到日期以服务器时区为准，避免时区差异导致的问题
4. **次卡逻辑**：只有`durationType === '0'`的学生才是次卡用户，其他类型学生不需要次卡功能
5. **删除保护**：删除签到记录时，需要确认操作，避免误删
6. **次卡管理**：次卡剩余次数需要管理员手动设置或通过其他方式管理（购买次卡功能暂不考虑）

---

## 十三、测试要点

1. **功能测试**：
   - 添加签到记录（正常、重复、次卡用户）
   - 删除签到记录（正常、次卡用户）
   - 查询签到记录（按日期、按学生）

2. **边界测试**：
   - 次卡次数为0时签到
   - 删除不存在的签到记录
   - 查询不存在的日期或学生

3. **数据一致性测试**：
   - 签到后次卡次数是否正确更新
   - 删除签到后次卡次数是否正确恢复

4. **性能测试**：
   - 大量签到记录的查询性能
   - 日历页切换日期的响应速度

---

## 总结

本设计方案提供了完整的签到功能实现方案，包括数据表设计、页面设计、云函数设计和数据管理流程。按照本方案实施，可以满足admin对学生的签到管理和次卡统计需求。

实施时建议分阶段进行，先实现核心功能，再逐步完善和优化。

